#include <iostream>
using namespace std;

/** works logically bs submit ni ho rha tha contest me pta ni kyu
 
 void findAns(long long &ans,int n, long k){
        if(k<=0)return ;
        int j = n-1;
        while(j>=0 && shop.get(j)>k){
            j--;
            // ans++;
        }
        if(j<0)return;
        ans++;
        findAns(ans,n,k- shop.get(j));
    }
    
    long long find(int n, long k){
        // Return the number of chocolates Geek had
        // enjoyed out of 'n' chocolates availabe in the
        // 'shop'.
        long long ans = 0;
        findAns(ans,n,k);
        return ans;
    }
 */